// Patron Gym OS - Service Worker
// Çevrimdışı çalışmayı ve önbelleklemeyi yönetir.
self.addEventListener('install', (e) => {
    console.log('[Service Worker] Kuruldu');
});

self.addEventListener('fetch', (e) => {
    // Basit fetch işlemi
});
